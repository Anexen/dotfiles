Clone repository in ~/.dotfiles

```bash
$ git clone https://github.com/Anexen/dotfiles.git ~/.dotfiles
```

Create symlinks:

```bash
$ show bash
$ show nvim
$ show git
```

Neovim config dependencies:
* aspell
* fd
* fzf
* ripgrep
